<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE chapter PUBLIC "-//OASIS//DTD DocBook XML//EN" "docbook.dtd"[
	<!ENTITY % xinclude SYSTEM "xinclude.mod">
	%xinclude;
]>

<chapter xmlns="http://docbook.org/ns/docbook" version="5.0" xml:id="printsetup-33230">



<title>Setting Up Printers (Tasks)</title>
<indexterm xml:id="printsetup-ix239">
<primary>printers</primary>
<secondary>setting up</secondary>
</indexterm>
<toc>
<para>This chapter describes how to set up a printer and how to make the printer accessible
to systems on the network by using Solaris Print Manager and the LP print service.</para>
<para>For information on the procedures associated with setting up printers by using
Solaris Print Manager, see <olink remap="internal" targetdoc="chapter-3.xml" targetptr="printsetup-2">Setting Up Printing (Task Map)</olink>.</para>
<para>For overview information about printing, see <olink remap="external" targetdoc="chapter-1.xml" targetptr="printconcept-76595">Chapter 1, Managing Printing Services (Overview)</olink>.</para>
</toc>
<sect1 xml:id="printsetup-2">
<title>Setting Up Printing (Task Map)</title>
<informaltable frame="all">
<tgroup cols="3" colsep="1" rowsep="1">
<colspec colname="colspec1" colwidth="33*"/>
<colspec colname="colspec2" colwidth="33*"/>
<colspec colname="colspec3" colwidth="33*"/>
<thead>
<row>
<entry>
<para>Task</para>
</entry>
<entry>
<para>Description</para>
</entry>
<entry>
<para>For Instructions</para>
</entry>
</row>
</thead>
<tbody>
<row>
<entry>
<para>Add a new attached printer by using Solaris Print Manager.</para>
</entry>
<entry>
<para>Use Solaris Print Manager to make the printer available for printing after you
physically attach the printer to a system. </para>
</entry>
<entry>
<para>
<olink remap="internal" targetdoc="chapter-3.xml" targetptr="ermly">How to Add a New Attached Printer by Using Solaris Print Manager</olink>
</para>
</entry>
</row>
<row>
<entry>
<para>Add a new attached printer by using LP print service commands.</para>
</entry>
<entry>
<para>Use LP print service commands to make the printer available for printing after
you physically attach the printer to a system. </para>
</entry>
<entry>
<para>
<olink remap="internal" targetdoc="chapter-3.xml" targetptr="ertru">How to Add a New Attached Printer by Using LP Print Service Commands</olink>
</para>
</entry>
</row>
<row>
<entry>
<para>Add access to a printer.</para>
</entry>
<entry>
<para>Use Solaris Print Manager to add printer access on the print client.</para>
</entry>
<entry>
<para>
<olink remap="internal" targetdoc="chapter-3.xml" targetptr="printsetup-9">How to Add Printer Access by Using Solaris Print Manager</olink>
</para>
</entry>
</row>
<row>
<entry>
<para> (Optional) Set up a <filename>.printers</filename> file.</para>
</entry>
<entry>
<para>Use a <filename>$HOME/.printers</filename> file so that users can establish
their own custom printer aliases. </para>
</entry>
<entry>
<para>
<olink remap="internal" targetdoc="chapter-3.xml" targetptr="printsetup-11">How to Set Up a <filename>.printers</filename> File</olink>
</para>
</entry>
</row>
<row>
<entry>
<para>Add a new network printer by using vendor-supplied tools.</para>
</entry>
<entry>
<para>Use Printer Vendor Supplied Tools to configure the network printer after you
physically connect the printer to the network. </para>
<para> After you physically connect the printer to the network, configure the network
printer.</para>
</entry>
<entry>
<para>
<olink remap="internal" targetdoc="chapter-3.xml" targetptr="printsetup-34420">How to Add a Network Printer by Using Printer Vendor-Supplied Tools</olink>
</para>
</entry>
</row>
<row>
<entry>
<para>Add a new network printer by using Solaris Print Manager.</para>
</entry>
<entry>
<para>Use Solaris Print Manager to make a printer available for printing over the
network.</para>
</entry>
<entry>
<para>
<olink remap="internal" targetdoc="chapter-3.xml" targetptr="eyasc">How to Add a New Network Printer by Using Solaris Print Manager</olink>
</para>
</entry>
</row>
<row>
<entry>
<para>Add a new network printer by using LP print service commands.</para>
</entry>
<entry>
<para>Use LP print service commands to make a printer available for printing over
the network.</para>
</entry>
<entry>
<para>
<olink remap="internal" targetdoc="chapter-3.xml" targetptr="ertsp">How to Add a New Network Printer by Using LP Print Service Commands</olink>
</para>
</entry>
</row>
<row>
<entry>
<para> Turn off banner pages.</para>
</entry>
<entry>
<para>You can optionally turn off banner pages so that the banner pages are never
printed.</para>
</entry>
<entry>
<para>
<olink remap="external" targetdoc="chapter-4.xml" targetptr="printadmin-55007">How to Turn Off Banner Pages</olink>
</para>
</entry>
</row>
<row>
<entry>
<para>Set up fault alerts.</para>
</entry>
<entry>
<para>You can optionally set up more specific fault alerts for the printer than the
fault alerts provided by Solaris Print Manager. </para>
</entry>
<entry>
<para>
<olink remap="external" targetdoc="chapter-4.xml" targetptr="printadmin-22606">How to Set Fault Alerts for a Printer</olink>
</para>
</entry>
</row>
<row>
<entry>
<para>Set up fault recovery.</para>
</entry>
<entry>
<para>You can optionally set up how a printer should recover after it faults. </para>
</entry>
<entry>
<para>
<olink remap="external" targetdoc="chapter-4.xml" targetptr="printadmin-10528">How to Set Printer Fault Recovery</olink>
</para>
</entry>
</row>
<row>
<entry>
<para>Limit access to a printer. </para>
</entry>
<entry>
<para>Solaris Print Manager enables you to optionally set up an allow list. If you
want to limit the access of a few users to the printer, you might want to set up a
deny list. </para>
</entry>
<entry>
<para>
<olink remap="external" targetdoc="chapter-4.xml" targetptr="printadmin-73366">How to Limit User Access to a Printer</olink>
</para>
</entry>
</row>
</tbody>
</tgroup>
</informaltable>
<sect2 xml:id="ermlj">
<title>Setting Up Printing by Using Solaris Print Manager</title>
<para>Solaris Print Manager is a Java-based graphical user interface that enables
you to manage local and remote printer configuration. This tool can be used in the
following name service environments: LDAP, NIS, NIS+, and files. You must be logged in as superuser to use this tool. </para>
<para>
<indexterm xml:id="ermme">
<primary>local printer</primary>
<secondary>task map for setting up</secondary>
</indexterm>
<indexterm xml:id="ermlx">
<primary>printers</primary>
<secondary>local</secondary>
</indexterm>
<indexterm xml:id="ermlu">
<primary>network printer</primary>
<secondary>task map for setting up</secondary>
</indexterm>
<indexterm xml:id="ermma">
<primary>printers</primary>
<secondary>network</secondary>
</indexterm>You can use Solaris Print Manager to set up print servers (Add New Attached
Printer or Add a Network Printer) and print clients (Add Access to Printer). An attached
printer is a printer that is physically cabled to the print server. A network printer
is physically attached to the network. You can use Solaris Print Manager to add an
attached printer or a network printer. Adding access to a printer, or adding remote
access, is the process of giving print clients (all those machines which are not the
server) access to the printer.</para>
</sect2>
<sect2 xml:id="ermll">
<title>Printer Setup With Solaris Print Manager</title>
<para>The following table describes each printer attribute to help you determine the
information that is needed to set up a printer with Solaris Print Manager.</para>
<informaltable frame="all" pgwide="1">
<tgroup cols="5" colsep="1" rowsep="1">
<colspec colname="colspec0" colwidth="80.91*"/>
<colspec colname="col2" colwidth="83.69*"/>
<colspec colname="colspec1" colwidth="70.60*"/>
<colspec colname="colspec2" colwidth="71.37*"/>
<colspec colname="col5" colwidth="89.40*"/>
<thead>
<row>
<entry>
<para>Printer Definition</para>
</entry>
<entry>
<para>Description</para>
</entry>
<entry>
<para>Example</para>
</entry>
<entry>
<para>Default Setting</para>
</entry>
<entry>
<para>Required or Optional?</para>
</entry>
</row>
</thead>
<tbody>
<row>
<entry>
<para>
<literal>Printer Name</literal>
</para>
</entry>
<entry>
<para>Name of printer</para>
</entry>
<entry>
<para>
<literal>laser1</literal>
</para>
</entry>
<entry>
<para>N/A</para>
</entry>
<entry>
<para>Required to install an attached or network printer and to add access to a printer</para>
</entry>
</row>
<row>
<entry>
<para>
<literal>Print server</literal>
</para>
</entry>
<entry>
<para>Name of print server</para>
</entry>
<entry>
<para>
<literal>venus</literal>
</para>
</entry>
<entry>
<para>N/A</para>
</entry>
<entry>
<para>Required to install an attached or network printer and to add access to a printer —
This field is filled in by the tool</para>
</entry>
</row>
<row>
<entry>
<para>
<literal>Description</literal>
</para>
</entry>
<entry>
<para>User defined string</para>
</entry>
<entry>
<para>
<literal>laser printer near breakroom</literal>
</para>
</entry>
<entry>
<para>N/A</para>
</entry>
<entry>
<para>Optional</para>
</entry>
</row>
<row>
<entry>
<para>
<literal>Printer Port</literal>
</para>
</entry>
<entry>
<para>Device that the printer is attached to</para>
</entry>
<entry>
<para>
<literal>/dev/term/a</literal>
</para>
</entry>
<entry>
<para>
<literal>/dev/term/a</literal>
</para>
</entry>
<entry>
<para>Required to install an attached printer</para>
</entry>
</row>
<row>
<entry>
<para>
<literal>Printer Type</literal>
</para>
</entry>
<entry>
<para>Type of printer</para>
</entry>
<entry>
<para>
<literal>unknown</literal>
</para>
</entry>
<entry>
<para>
<literal>PostScript</literal>
</para>
</entry>
<entry>
<para>Required to install an attached printer or a network printer without PPD files</para>
</entry>
</row>
<row>
<entry>
<para>
<literal>File Content Type</literal>
</para>
</entry>
<entry>
<para>Content to be printed</para>
</entry>
<entry>
<para>
<literal>any</literal>
</para>
</entry>
<entry>
<para>
<literal>PostScript</literal>
</para>
</entry>
<entry>
<para>Required to install an attached printer or a network printer without PPD files</para>
</entry>
</row>
<row>
<entry>
<para>
<literal>Printer Make</literal>
</para>
</entry>
<entry>
<para>Make of printer</para>
</entry>
<entry>
<para>Lexmark</para>
</entry>
<entry>
<para>N/A</para>
</entry>
<entry>
<para>Required to install an attached printer or a network printer with PPD files</para>
</entry>
</row>
<row>
<entry>
<para>
<literal>Printer Model</literal>
</para>
</entry>
<entry>
<para>Model of printer</para>
</entry>
<entry>
<para>Lexmark Optra E312</para>
</entry>
<entry>
<para>N/A</para>
</entry>
<entry>
<para>Required to install an attached printer or a network printer with PPD files</para>
</entry>
</row>
<row>
<entry>
<para>
<literal>Printer Driver</literal>
</para>
</entry>
<entry>
<para>Driver that is used by specifying PPD file</para>
</entry>
<entry>
<para>Foomatic/PostScript</para>
</entry>
<entry>
<para>N/A</para>
</entry>
<entry>
<para>Required to install an attached printer or a network printer with PPD files</para>
</entry>
</row>
<row>
<entry>
<para>
<literal>Destination</literal>
</para>
</entry>
<entry>
<para>Destination name for the network printer</para>
</entry>
<entry>
<para>For examples, see <olink remap="internal" targetdoc="chapter-3.xml" targetptr="printconcept-22">Selecting the Destination Name (Also Called the Network Printer Access Name)</olink>
</para>
</entry>
<entry>
<para>N/A</para>
</entry>
<entry>
<para>Required to install a network printer</para>
</entry>
</row>
<row>
<entry>
<para>
<literal>Protocol</literal>
</para>
</entry>
<entry>
<para>Protocol used to communicate with the printer</para>
</entry>
<entry>
<para>
<literal>TCP</literal>
</para>
</entry>
<entry>
<para>
<literal>BSD</literal>
</para>
</entry>
<entry>
<para>Required to install a network printer </para>
</entry>
</row>
<row>
<entry>
<para>
<literal>Fault Notification</literal>
</para>
</entry>
<entry>
<para>Specifies how to notify user of errors</para>
</entry>
<entry>
<para>
<literal>Mail to superuser</literal>
</para>
</entry>
<entry>
<para>
<literal>Write to superuser</literal>
</para>
</entry>
<entry>
<para>Optional</para>
</entry>
</row>
<row>
<entry>
<para>Default Printer</para>
</entry>
<entry>
<para>Identifies the default printer</para>
</entry>
<entry>
<para>N/A</para>
</entry>
<entry>
<para>N/A</para>
</entry>
<entry>
<para>Optional</para>
</entry>
</row>
<row>
<entry>
<para>Banner Page Drop-Down Menu:</para>
<itemizedlist>
<listitem>
<para>
<literal>Always Print Banner</literal>
</para>
</listitem>
<listitem>
<para>
<literal>User Selectable-Default=on</literal>
</para>
</listitem>
<listitem>
<para>
<literal>Never Print Banner</literal>
</para>
</listitem>
</itemizedlist>
</entry>
<entry>
<para>
</para>
<para>Print banner?</para>
<para>
</para>
<para>
</para>
<para>Print banner? User selectable option, turned on by default. To turn banner printing
off, use <command>lp</command> command with appropriate option.</para>
<para>Never print banner?</para>
</entry>
<entry>
<para>N/A</para>
</entry>
<entry>
<para>
</para>
<para>Banner is printed</para>
<para>
</para>
<para>
</para>
<para>
</para>
<para>
</para>
<para>Banner is printed unless turned off by using <command>lp</command> command with
appropriate option</para>
<para>
</para>
<para>
</para>
<para>Banner is not printed</para>
</entry>
<entry>
<para>Banner page printing turned on by default unless another option is specified
within the Solaris Print Manager pull-down menu</para>
</entry>
</row>
<row>
<entry>
<para>
<literal>User Access List</literal>
</para>
</entry>
<entry>
<para>List of users allowed to print on the print server</para>
</entry>
<entry>
<para>
<literal>rimmer,lister</literal>
</para>
</entry>
<entry>
<para>All users can print</para>
</entry>
<entry>
<para>Optional</para>
</entry>
</row>
</tbody>
</tgroup>
</informaltable>
</sect2>
<sect2 xml:id="ermlv">
<title>Solaris Print Manager and Special Characters</title>
<para>Solaris Print Manager checks user input for the various text fields in the input
screens. There are two types of checking: general illegal input and input that is
illegal for specific fields.</para>
<para>Solaris Print Manager does not accept the following characters as input, except
for the help screens:</para>
<itemizedlist>
<listitem>
<para>Shell metacharacters, such as “\$^&amp;*(){}`'|;:?&lt;&gt;, except
for the destination field on the network printer screen, which accepts colons (:)</para>
</listitem>
<listitem>
<para>Multibyte characters</para>
</listitem>
<listitem>
<para>Pound signs (#), spaces, or tabs, except the description field, which
accepts tabs</para>
</listitem>
</itemizedlist>
</sect2>
</sect1>
<sect1 xml:id="printsetup-5">
<title>Starting Solaris Print Manager</title>
<para>To start Solaris Print Manager, either select Printer Administrator from the
CDE Workspace menu or start the Print Manager from the command line. See the following
procedure for details.</para>
<sect2>
<title/>
<task id="printsetup-6">

<title>How to Start Solaris Print Manager</title>
<indexterm xml:id="indexterm-30">
<primary>printers</primary>
</indexterm>
<indexterm xml:id="indexterm-31">
<primary>Solaris Print Manager</primary>
</indexterm>
<indexterm xml:id="indexterm-32">
<primary>starting</primary>
</indexterm>
<procedure>
<step xml:id="printsetup-step-1">
<para>
<indexterm xml:id="printsetup-ix264">
<primary>Solaris Print Manager</primary>
<secondary>prerequisites for using</secondary>
</indexterm>Verify that the following prerequisites are met. To use Solaris Print
Manager, you must meet the following requirements: </para>
<itemizedlist>
<listitem>
<para>Have a bitmapped display monitor, such as the standard display monitor
that comes with a Sun workstation. Solaris Print Manager can be used only on a system
with a console that is a bitmapped screen.</para>
</listitem>
<listitem>
<para>Be running an X Window System, such as the CDE environment, or be
using the remote display feature on a system running an <command>xhost</command> environment.</para>
</listitem>
<listitem>
<para>Be logged in as superuser or assume an equivalent role on the print
server to install an attached or network printer, or on the print client to add access
to a printer.</para>
</listitem>
<listitem>
<para>Have the required access privileges for managing
the LDAP, NIS, or NIS+ database.</para>
<itemizedlist>
<listitem>
<para>If your name service is LDAP, you must have the following items.</para>
<itemizedlist>
<listitem>
<para>The distinguished name (DN) of a printer administrator and password
in the directory.</para>
</listitem>
<listitem>
<para>The name or IP address of an LDAP server hosting the administered
domain. This information is usually displayed and selected automatically.</para>
</listitem>
</itemizedlist>
</listitem>
<listitem>
<para>If your name service is NIS, you must have the root password for the
NIS master.</para>
</listitem>
<listitem>
<para>If you name service is NIS+, you might need to complete the following
steps.</para>
<orderedlist>
<listitem>
<para>Log in to the NIS+ master as superuser.</para>
</listitem>
<listitem>
<para>Identify the group that owns the printers table.</para>
<screen># <userinput>niscat -o printers.org_dir.</userinput>
<replaceable>domain-name</replaceable>
<userinput>.com</userinput>
            .
            .
            .
         Group		: "admin.<replaceable>domain-name</replaceable>.com"</screen>
</listitem>
<listitem>
<para>If necessary, add the system that runs Solaris Print Manager to the
NIS+ admin group authorized to update the <filename>printers.org_dir.</filename>
<replaceable>&lt;domain&gt;
</replaceable> file.</para>
<screen># <userinput>nisgrpadm -a admin.</userinput>
<replaceable>domain-name</replaceable>
<userinput>.com</userinput> <replaceable>hostname</replaceable>
</screen>
</listitem>
<listitem>
<para>As superuser, log in to the system that runs Solaris Print Manager.
Your NIS+ configuration might make it necessary to run the <command>/usr/bin/keylogin</command> command. For more information, see the <olink remap="external" targetdoc="819-2239" targetptr="keylogin-1">
<citerefentry>
<refentrytitle>keylogin</refentrytitle>
<manvolnum>
1
</manvolnum>
</citerefentry>
</olink> man
page.</para>
</listitem>
</orderedlist>
</listitem>
</itemizedlist>
</listitem>
<listitem>
<para>Have the <literal>SUNWppm</literal> package installed.</para>
<screen># <userinput>pkginfo | grep SUNWppm</userinput>
system      SUNWppm        Solaris Print Manager</screen>
</listitem>
</itemizedlist>
</step>
<step xml:id="printsetup-step-2">
<para>Start Solaris Print Manager by using one of the
following methods:</para>
<itemizedlist>
<listitem>
<para>Select Printer Administrator from the Tools option of the CDE Workspace
menu. </para>
</listitem>
<listitem>
<para>Select the Applications menu from the CDE front panel. Then, click
the Printer Administrator icon in the Application Manager's System Admin window.</para>
</listitem>
<listitem>
<para>Type the following command.</para>
<screen># <userinput>/usr/sadm/admin/bin/printmgr &amp;</userinput>
</screen>
<para>The Select Naming Service window overlays the Solaris Print Manager main window.</para>
</listitem>
<listitem>
<para>If you want to use Solaris Print Manager from a remote system, use
the <command>xhost</command> command on the local system to give the remote system
display access.</para>
<screen># <userinput>xhost +</userinput>
<replaceable>remote-system</replaceable>
</screen>
<para>Then log in to the remote system, set the <literal>DISPLAY</literal> environment
variable, and start Solaris Print Manager:</para>
<screen># <userinput>DISPLAY=</userinput>
<replaceable>local-system:display-number</replaceable>
# <userinput>export DISPLAY</userinput>
# <userinput>/usr/sadm/admin/bin/printmgr &amp;</userinput>
</screen>
</listitem>
</itemizedlist>
<note>

<para>If Solaris Print Manager fails to start from the CDE menu or from the
command line, check the following: </para>
<itemizedlist>
<listitem>
<para>Superuser (<literal>root</literal>) might not have permission to connect
to the X-server process on the local system or the remote system. In this case, type
the following:</para>
<screen>$ <userinput>xhost +</userinput>
<replaceable>hostname</replaceable>
$ <userinput>su</userinput>
(<emphasis>Enter root's password</emphasis>)
# <userinput>/usr/sadm/admin/bin/printmgr &amp;</userinput>
</screen>
<para>Replace <replaceable>hostname</replaceable> with either the local system name
or the remote system name before restarting Solaris Print Manager.</para>
</listitem>
<listitem>
<para>Verify that the <literal>SUNWppm</literal> package is installed on
the local system or the remote system.</para>
<screen>$ <userinput>pkginfo | grep SUNWppm</userinput>
</screen>
</listitem>
</itemizedlist>
</note>
</step>
<step xml:id="printsetup-step-3">
<para>Select the name service that is used in your network
from the Select Naming Service window. The choices are:  LDAP,
NIS, NIS+, or files.     </para>
</step>
<step xml:id="printsetup-step-4">
<para>Check that the domain name is correct. </para>
<para>The Solaris Print Manager main menu is displayed after the name service is loaded
successfully. </para>
</step>
</procedure>
</task>
</sect2>
</sect1>
<sect1 xml:id="printsetup-20070">
<title>Setting Up a Print Server</title>
<indexterm xml:id="printsetup-ix274">
<primary>print server</primary>
<secondary>setting up (overview)</secondary>
</indexterm>
<indexterm xml:id="printsetup-ix275">
<primary>print server</primary>
<secondary>definition</secondary>
</indexterm>
<para>When you add an attached printer or a network printer to a system, the printer
is made accessible to the local system. The system on which you install the printer
becomes the <emphasis>print server</emphasis>.</para>
<para>The following procedure describes how to use Solaris Print Manager to add a
new attached printer with the expanded printer support that is the default in this
release.</para>
<para>
<indexterm xml:id="indexterm-33">
<primary>Expanded Printer Support</primary>
<secondary>adding an attached printer with PPD files by using Solaris Print Manager</secondary>
</indexterm>For overview information about the new or changed Solaris Print Manager
features in this release, see <olink remap="external" targetdoc="chapter-1.xml" targetptr="printconcept-42">What's New in Printing?</olink>.</para>
<sect2>
<title/>
<task id="ermly">

<title>How to Add a New Attached Printer by Using Solaris Print Manager</title>
<tasksummary>
<para>The printer definitions that you can assign with Solaris Print Manager include
the use of PPD files. The <literal>Use PPD files</literal> option is available in
the Print Manager drop-down menu in Solaris Print Manager. This default option enables
you to select the printer make, model, and driver when adding new printer.</para>
<para>This procedure shows you how to add a new attached printer by using Solaris
Print Manager with the Use PPD files default option selected. To add a new attached
printer without PPD files, you must first deselect this option in the Print Manager
drop-down menu. For overview information about new or modified Solaris Print Manager
features in this release, see <olink remap="external" targetdoc="chapter-1.xml" targetptr="printconcept-42">What's New in Printing?</olink>.</para>
</tasksummary>
<procedure xml:id="ermlp">
<step>
<para>Select the system to be the print server.</para>
</step>
<step>
<para>Connect the printer to the print server. Turn on the power to the printer.</para>
<para>Consult the printer vendor's installation documentation for information
about the hardware switches and cabling requirements. </para>
</step>
<step>
<para>Start Solaris Print Manager on the print server where you connected the
printer.</para>
<para>For instructions, see <olink remap="internal" targetdoc="chapter-3.xml" targetptr="printsetup-6">How to Start Solaris Print Manager</olink>.</para>
</step>
<step>
<para>Select New Attached Printer from the Printer menu.</para>
<para>The New
Attached Printer window is displayed.</para>
</step>
<step>
<para>Fill in the required fields with the appropriate information.</para>
<para>
<indexterm xml:id="indexterm-34">
<primary>local or attached printer</primary>
<secondary>adding by using Solaris Print Manager (how to)</secondary>
</indexterm>
<indexterm xml:id="indexterm-35">
<primary>printers</primary>
<secondary>adding local or attached (how to)</secondary>
<tertiary>adding by using Solaris Print Manager</tertiary>
</indexterm>
<indexterm xml:id="indexterm-36">
<primary>adding</primary>
<secondary>local or attached printers (how to)</secondary>
</indexterm>If you need information to complete a field, click the Help button.</para>
<para>For example, to add a new attached printer with PPD files, provide the appropriate
information in the following fields:</para>
<screen>Printer Name:
Description:
Printer Port:
Printer Make:
Printer Model:
Printer Driver:
Fault Notification:
Options:
Default Printer
Always Print Banner
User Access List:</screen>
<para>The Printer Server field is filled in by Solaris Print Manager.</para>
<para>To add a new attached printer without PPD files, provide the appropriate information
in the following fields:</para>
<screen>Printer Name:
Description:
Printer Port:
Printer Type:
File Contents:
Fault Notification:
Options:
Default Printer
Always Print Banner
User Access List:</screen>
<para>The Printer Server field is filled in by Solaris Print Manager.</para>
</step>
<step>
<para>Click OK.</para>
</step>
<step>
<para>Verify that the printer has been installed. Check for the new printer
entry in the Solaris Print Manager main window.</para>
</step>
<step>
<para>Verify that the printer can print requests.</para>
<screen>$ <userinput>lp -d</userinput> <replaceable>printer-name</replaceable> <replaceable>filename</replaceable>
</screen>
</step>
<step>
<para>Exit Solaris Print Manager.</para>
<para>Choose Exit from the Print Manager
Menu.</para>
</step>
</procedure>
<taskrelated>

<title>Verifying That the New Solaris Print Manager Feature Is
Working</title>
<para>When you are using Solaris Print Manager to create or modify a print queue,
choose the <literal>files</literal> option as the name service. Select the Printer
-&gt; New Attached Printer option from the menu. If the drop-down menu displays printer
make and printer model, the new feature is working.</para>
</taskrelated>
<taskrelated>
<title>Verifying That the RIP Feature Is Working</title>
<para>The RIP enables you to print to printers that do not have resident PostScript
processing  capabilities.</para>
<para>To ensure that the RIP feature is working you need to create a new print queue
by selecting one of the make/model combinations that is available in the New Attached
Printer and New Network Printer screens used by Solaris Print Manager. Then, try printing
to the new printer. If the output of the print job is recognizable, the RIP feature
is working. If the output of the print job is unrecognizable, then the RIP feature
is not working.</para>
</taskrelated>
</task>
</sect2>
<sect2>
<title/>
<task id="ertru">

<title>How to Add a New Attached Printer by Using LP Print Service
Commands</title>
<tasksummary>
<para>
<indexterm xml:id="indexterm-37">
<primary>Expanded Printer Support</primary>
<secondary>adding an attached printer with PPD files by using <command>lp</command> commands</secondary>
</indexterm>
<indexterm xml:id="indexterm-38">
<primary>printer</primary>
<secondary>adding a new (how to)</secondary>
<tertiary>with Solaris Print Manager</tertiary>
</indexterm>
<indexterm xml:id="indexterm-39">
<primary>adding</primary>
<secondary>printer, new (how to)</secondary>
<tertiary>with Solaris Print Manager</tertiary>
</indexterm>
<indexterm xml:id="indexterm-40">
<primary>Solaris Print Manager</primary>
<secondary>adding a new printer (how to)</secondary>
</indexterm>This procedure describes how to add a new attached printer with expanded
printer support. The <option>
n</option> option to the <command>lpadmin</command> command
enables you to specify a PPD file when adding a new attached printer or modifying
an existing attached printer. Two examples follow this procedure. The first example
shows you how to add a new attached printer with PPD files. The second example shows
you how to add a new attached printer without PPD files.</para>
</tasksummary>
<procedure xml:id="ertrv">
<step>
<para>Connect the printer to the system, and turn  on the power to the printer.</para>
<para>Consult the printer vendor's installation documentation for information
about the hardware switches and cabling requirements.</para>
</step>
<step>
<para>Collect the information that is required to configure an attached printer
with PPD files.</para>
<itemizedlist>
<listitem>
<para>Printer name and port device</para>
</listitem>
<listitem>
<para>File content type</para>
</listitem>
<listitem>
<para>PPD file</para>
</listitem>
</itemizedlist>
<para>To determine the PPD file that the printer will use, first define the printer
make, model, and driver.</para>
<para>For overview information about using PPD files
when adding a printer or modifying an existing printer by using LP print service commands,
see <olink remap="external" targetdoc="chapter-1.xml" targetptr="printconcept-42">What's New in Printing?</olink>.</para>
</step>
<step>
<para>Define the printer name, port device, file content type, and PPD file
that the printer will use.</para>
<para>When using PPD files, the file content type
is typically PostScript.</para>
<substeps>
<step>
<para>Specify the printer name and the port device that the printer will use.</para>
<screen># <userinput>lpadmin -p <replaceable>printer-name</replaceable> -v /dev/printers/0</userinput>
</screen>
<para>The device to use is <filename>/dev/printers/0</filename>.</para>
</step>
<step>
<para>Specify the interface script that the printer will use.</para>
<screen># <userinput>lpadmin -p <replaceable>printer-name</replaceable> -m standard_foomatic</userinput>
</screen>
</step>
<step>
<para>Specify the file content type and the PPD file that the printer will use.</para>
<screen># <userinput>lpadmin -p <replaceable>printer-name</replaceable> -I <replaceable>content-type</replaceable> -n /<replaceable>path</replaceable>/<replaceable>ppdfile</replaceable>
</userinput>
</screen>
<para>The PPD file that you supply is located in the <filename>/<replaceable>path</replaceable>
</filename> directory.</para>
</step>
<step>
<para>Specify the printer description.</para>
<screen># <userinput>lpadmin -p <replaceable>printer-name</replaceable> -D "<replaceable>printer-description</replaceable>"</userinput>
</screen>
</step>
</substeps>
<para>For more information, see the <olink remap="external" targetdoc="819-2240" targetptr="lpadmin-1m">
<citerefentry>
<refentrytitle>lpadmin</refentrytitle>
<manvolnum>
1M
</manvolnum>
</citerefentry>
</olink> man
page.</para>
</step>
<step>
<para>(Optional) Add filters to the print server.</para>
<screen># <userinput>cd /etc/lp/fd</userinput>
# <userinput>for</userinput> <replaceable>filter</replaceable> <userinput>in *.fd;do</userinput>
    &gt; <replaceable>name</replaceable>
<userinput>=`basename $</userinput>
<replaceable>filter</replaceable> <userinput>.fd`</userinput>
    &gt; <userinput>lpfilter -f $</userinput> <replaceable>name</replaceable>
<userinput>-F $</userinput> <replaceable>filter</replaceable>
    &gt; <userinput>done</userinput>
</screen>
</step>
<step>
<para>Enable the printer to accept print requests and to print those requests.</para>
<screen># <userinput>accept</userinput> <replaceable>printer-name</replaceable>
# <userinput>enable</userinput> <replaceable>printer-name</replaceable>
</screen>
</step>
<step>
<para>Verify that the printer is correctly configured.</para>
<screen># <userinput>lpstat -p <replaceable>printer-name</replaceable> -l</userinput>
</screen>
<para>The output of the <command>lpstat</command> command will list the PPD file that
you used.</para>
</step>
</procedure>
<example xml:id="ertrx">

<title>Adding a New Attached Printer With PPD Files by Using LP Print Service Commands</title>
<indexterm xml:id="indexterm-41">
<primary>Expanded Printer Support</primary>
<secondary>adding a printer by using <command>lpadmin</command> <option>
n</option> command</secondary>
<tertiary>example of</tertiary>
</indexterm>
<para>This example shows how to add a new attached printer with PPD files. The <option>
n</option> option to the <command>lpadmin</command> command enables you to add a new
print queue by specifying PPD files. The following information is used as an example.
The information that you provide will vary:</para>
<itemizedlist>
<listitem>
<para>Printer name: <literal>paper</literal>
</para>
</listitem>
<listitem>
<para>Port device: <literal>/dev/printers/0</literal>
</para>
</listitem>
<listitem>
<para>File content type: <literal>postscript</literal>
</para>
</listitem>
<listitem>
<para>PPD file: <filename>/<replaceable>path</replaceable>/Lexmark-Optra_E312-Postscript.ppd.gz</filename>
</para>
</listitem>
</itemizedlist>
<screen># <userinput>lpadmin -p paper -v /dev/printers/0</userinput> <lineannotation>(1)</lineannotation>
# <userinput>lpadmin -p paper -I postscript</userinput> <lineannotation>(2)</lineannotation>
# <userinput>lpadmin -p paper -m standard_footmatic</userinput> <lineannotation>(3)</lineannotation>
# <userinput>lpadmin -p paper -n /<replaceable>path</replaceable>/Lexmark-Optra_E312-Postscript.ppd.gzmake</userinput>
<lineannotation>(4)</lineannotation>
# <userinput>lpadmin -p paper -D "Color printer on third floor, rm 3003"</userinput> <lineannotation>(5)</lineannotation>
# <userinput>cd /etc/lp/fd</userinput>
# <userinput>for</userinput> <replaceable>filter</replaceable> <userinput>in *.fd;do</userinput> 
   &gt; <replaceable>name</replaceable>
<userinput>=`basename $</userinput> <replaceable>filter</replaceable> <userinput>.fd`</userinput>
   &gt; <userinput>lpfilter -f $</userinput> <replaceable>name</replaceable> <userinput>-F $</userinput> <replaceable>filter</replaceable> 
   &gt; <userinput>done</userinput> <lineannotation>(6)</lineannotation>
# <userinput>accept paper</userinput>
	destination “paper” now accepting requests 
# <userinput>enable paper</userinput>	<lineannotation>(7)</lineannotation> 
printer “paper” now enabled 
# <userinput>lpstat -p paper</userinput> <lineannotation>(8)</lineannotation>
printer paper is idle. enabled since Feb 28 11:21 2004...</screen>
<orderedlist>
<listitem>
<para>Defines the printer name and the port device that the printer will
use</para>
</listitem>
<listitem>
<para>Sets the file content types to which the printer can print to directly</para>
</listitem>
<listitem>
<para>Specifies the interface script for utilizing PPD files</para>
</listitem>
<listitem>
<para>Specifies the path to the PPD file and the PPD file name</para>
</listitem>
<listitem>
<para>Adds a description for the printer</para>
</listitem>
<listitem>
<para>Adds print filters to the print server</para>
</listitem>
<listitem>
<para>Accepts print requests for the printer and enables the printer</para>
</listitem>
<listitem>
<para>Verifies that the printer is ready for printing</para>
</listitem>
</orderedlist>
</example>
<example xml:id="ertrw">

<title>Adding a New Attached Printer Without PPD Files by Using LP Print Service Commands</title>
<para>This example shows how to add a new attached printer without PPD files. The
commands in this example must be executed on the print server where the printer is
connected. The following information is used as an example. The information that you
provide will vary:</para>
<itemizedlist>
<listitem>
<para>Printer name: <literal>luna</literal>
</para>
</listitem>
<listitem>
<para>Port device: <literal>/dev/term/b</literal>
</para>
</listitem>
<listitem>
<para>Printer type: <literal>PS</literal>
</para>
</listitem>
<listitem>
<para>File content type: <literal>postscript</literal>
</para>
<screen># <userinput>chown lp /dev/term/b</userinput>
# <userinput>chmod 600 /dev/term/b</userinput> <lineannotation>(1)</lineannotation>
# <userinput>lpadmin -p luna -v /dev/term/b</userinput> <lineannotation>(2)</lineannotation>
# <userinput>lpadmin -p luna -T PS</userinput> <lineannotation>(3)</lineannotation>
# <userinput>lpadmin -p luna -I postscript</userinput> <lineannotation>(4)</lineannotation>
# <userinput>lpadmin -p luna -D "Room 1954 ps"</userinput> <lineannotation>(5)</lineannotation>
# <userinput>cd /etc/lp/fd</userinput>
# <userinput>for</userinput> <replaceable>filter</replaceable> <userinput>in *.fd;do</userinput> 
   &gt; <replaceable>name</replaceable> <userinput>=`basename $</userinput> <replaceable>filter</replaceable>
<userinput>.fd`</userinput>
   &gt; <userinput>lpfilter -f $</userinput> <replaceable>name</replaceable> <userinput>-F $</userinput> <replaceable>filter</replaceable>
   &gt; <userinput>done</userinput> <lineannotation>(6)</lineannotation>
# <userinput>accept luna</userinput>
destination “luna” now accepting requests 
# <userinput>enable luna</userinput> <lineannotation>(7)</lineannotation> 
printer “luna” now enabled 
# <userinput>lpstat -p luna</userinput> <lineannotation>(8)</lineannotation>
printer luna is idle.enabled since Feb 28 11:21 2003.available</screen>
</listitem>
</itemizedlist>
<orderedlist>
<listitem>
<para>Gives <literal>lp</literal> ownership and sole access to a port device</para>
</listitem>
<listitem>
<para>Defines the printer name and the port device that the printer will
use</para>
</listitem>
<listitem>
<para>Sets the printer type of the printer</para>
</listitem>
<listitem>
<para>Specifies the file content types to which the printer can print directly</para>
</listitem>
<listitem>
<para>Adds a description for the printer</para>
</listitem>
<listitem>
<para>Adds print filters to the print server</para>
</listitem>
<listitem>
<para>Accepts print requests for the printer and enables the printer</para>
</listitem>
<listitem>
<para>Verifies that the printer is ready for printing</para>
</listitem>
</orderedlist>
</example>
<taskrelated role="see-also">

<itemizedlist>
<listitem>
<para>
<olink remap="internal" targetdoc="chapter-3.xml" targetptr="printsetup-9">How to Add Printer Access by Using Solaris Print Manager</olink> to add printer access on a print client.</para>
</listitem>
<listitem>
<para>
<olink remap="internal" targetdoc="chapter-3.xml" targetptr="printsetup-11">How to Set Up a <filename>.printers</filename> File</olink> to
allow users to customize printer aliases.</para>
</listitem>
</itemizedlist>
</taskrelated>
</task>
</sect2>
</sect1>
<sect1 xml:id="ermli">
<title>Setting Up a Print Client</title>
<para>A <emphasis>print client</emphasis> is a system that is not the server for the
printer. Yet, this system has access to the printer. A print client uses the services
of the print server to spool, schedule, and filter the print jobs. Note that one system
can be a print server for one printer and be a print client for another printer.</para>
<para>Access to a printer can be configured on a domain-wide basis or on a per-machine
basis. If you add the printer information to the name service database, access is
configured on a domain-wide basis.</para>
<para>The following procedure describes how to use the Solaris Print Manager to add
access to a printer on a print client. The example that follows describes how to add
printer access by using the <command>lp</command> print service commands.</para>
<sect2>
<title/>
<task id="printsetup-9">

<title>How to Add Printer Access by Using Solaris Print Manager</title>
<procedure>
<step xml:id="printsetup-step-13">
<para>
<indexterm xml:id="printsetup-ix298">
<primary>adding</primary>
<secondary>access to remote printers</secondary>
</indexterm>
<indexterm xml:id="indexterm-42">
<primary>Solaris Print Manager</primary>
<secondary>adding printer access</secondary>
</indexterm>Start Solaris Print Manager on the system where you want to add access
to a remote printer.</para>
<para>For instructions, see <olink remap="internal" targetdoc="chapter-3.xml" targetptr="printsetup-6">How to Start Solaris Print Manager</olink>.</para>
</step>
<step xml:id="printsetup-step-14">
<para>Select Add Access to Printer from the Printer
menu. </para>
<para>The Add Access to Printer window is displayed.</para>
</step>
<step xml:id="printsetup-step-15">
<para>Type the appropriate data in the entry fields.</para>
<para>If you need information to complete a field, click the Help button.</para>
</step>
<step xml:id="printsetup-step-16">
<para>Click OK.</para>
</step>
<step xml:id="printsetup-step-17">
<para>Verify that access to the printer is added by
checking for the new printer entry in the Solaris Print Manager main window.</para>
</step>
<step xml:id="printsetup-step-117">
<para>Verify that the printer can print requests.</para>
<screen>$ <userinput>lp -d</userinput> <replaceable>printer-name</replaceable> <replaceable>filename</replaceable>
</screen>
</step>
<step xml:id="printsetup-step-18">
<para>Exit Solaris Print Manager.</para>
<para>Choose
Exit from the Print Manager Menu.</para>
</step>
</procedure>
<example xml:id="emjgc">

<title>Adding Printer Access by Using <command>lp</command> Print Service Commands</title>
<para>If you want to print to a remote printer, you must add access to the remote
printer. This example shows how to configure access to a printer named <literal>luna</literal>, whose print server is <literal>saturn</literal>. The system <literal>saturn</literal> becomes a print client of the printer <literal>luna</literal>.</para>
<screen># <userinput>lpadmin -p luna -s saturn</userinput> <lineannotation>(1)</lineannotation>
# <userinput>lpadmin -p luna -D "Room 1954 ps"</userinput> <lineannotation>(2)</lineannotation>
# <userinput>lpadmin -d luna</userinput> <lineannotation>(3)</lineannotation>
# <userinput>lpstat -p luna</userinput> <lineannotation>(4)</lineannotation>
</screen>
<orderedlist>
<listitem>
<para>Identifies the printer and the print server</para>
</listitem>
<listitem>
<para>Adds a description for the printer</para>
</listitem>
<listitem>
<para>Sets the printer as the system's default printer destination</para>
</listitem>
<listitem>
<para>Verifies that the printer is ready for printing</para>
</listitem>
</orderedlist>
</example>
</task>
</sect2>
</sect1>
<sect1 xml:id="printsetup-10">
<title>Setting Up a <filename>.printers</filename> File</title>
<para>There is no need to set up a <filename>.printers</filename> file in your users'
home directories if the users don't need customized printer information. However,
the <filename>.printers</filename> file enables users to establish their own printer
aliases. You can use the <literal>_default</literal> alias to make a printer the default
printer. You can also set up a special <literal>_all</literal> alias to define a list
of printers affected when you cancel a print request or to check the status of printers. </para>
<para>The use of the <filename>.printers</filename> file by the LP print service is
controlled by the name service switch (<filename>/etc/nsswitch.conf</filename>). The
default configuration specifies that the print service checks a user's home directory
to locate printer configuration information before it checks the other name services.
So, you can tailor a user's printer configuration file to use custom printer information
rather than the shared information in the name service. </para>
<para>For more information about the <filename>.printers</filename> file, see the<olink remap="external" targetdoc="819-2251" targetptr="printers-4">
<citerefentry>
<refentrytitle>printers</refentrytitle>
<manvolnum>
4
</manvolnum>
</citerefentry>
</olink> man page. For more information about
the name service switch, see the <olink remap="external" targetdoc="819-2251" targetptr="nsswitch.conf-4">
<citerefentry>
<refentrytitle>nsswitch.conf</refentrytitle>
<manvolnum>
4
</manvolnum>
</citerefentry>
</olink> man
page.</para>
<sect2>
<title/>
<task id="printsetup-11">

<title>How to Set Up a <filename>.printers</filename> File</title>
<procedure>
<step xml:id="printsetup-step-30">
<para>Log in to the user's system as superuser or assume
an equivalent role.</para>
</step>
<step xml:id="printsetup-step-31">
<para>Start the file editor that you want to use to
create a <filename>.printers</filename> file in the user's home directory. </para>
</step>
<step xml:id="printsetup-step-32">
<para>
<indexterm xml:id="indexterm-43">
<primary>
<filename>.printers</filename> file</primary>
<secondary>setting up (how to)</secondary>
</indexterm>(Optional) Set up the <literal>_default</literal> alias to make a specific
printer the user's default printer. Use an entry that is similar to this entry.</para>
<screen>_default <replaceable>printer-name</replaceable>
</screen>
</step>
<step xml:id="printsetup-step-33">
<para>(Optional) Set up the <literal>_all</literal> alias
to define the printers affected when you cancel a print request or check the status
of printers. Use an entry similar to this entry.</para>
<screen>_all <replaceable>printer1,printer2,printer3</replaceable>
</screen>
</step>
<step xml:id="printsetup-step-34">
<para>Save the file as <filename>.printers</filename>. </para>
</step>
</procedure>
</task>
</sect2>
</sect1>
<sect1 xml:id="printsetup-30">
<title>Introduction to Adding a Network Printer</title>
<indexterm xml:id="printsetup-ix292">
<primary>network printer</primary>
<secondary>adding, deleting</secondary>
</indexterm>
<para>
<indexterm xml:id="printsetup-ix293">
<primary>network printer</primary>
<secondary>defined</secondary>
</indexterm>A <emphasis>network printer</emphasis> is a hardware device that is directly
connected to the network. This printer can be accessed from a print server without
actually connecting the printer to the print server with a cable. The network printer
has its own system name and IP address.</para>
<caution>

<para>Configuring a network printer without utilizing queue configuration
on a print server is an unsupported method that results in printer misconfiguration.</para>
<para>You can correctly configure a network printer with Solaris Print Manager
by selecting the New Network Printer option. Or, you can correctly configure a network
printer by using the <command>lpadmin</command> command. For more information on how
to configure a network printer by using the <command>lpadmin</command> command, see <olink remap="internal" targetdoc="chapter-3.xml" targetptr="ertsp">How to Add a New Network Printer by Using LP Print Service Commands</olink>.</para>
<para>Note that you should not use the <command>lpadmin</command> <option>
p</option> <replaceable>queue-name</replaceable> <option>
s</option> <replaceable>printer-name</replaceable> command or the Add Access to Printer option of Solaris
Print Manager to add a network printer. These methods should be used exclusively to
point to an already existing queue on a remote print server.</para>
<para>The print
server provides the following:</para>
<itemizedlist>
<listitem>
<para>Queueing capabilities</para>
</listitem>
<listitem>
<para>Filtering</para>
</listitem>
<listitem>
<para>Printing administration for a network printer</para>
</listitem>
</itemizedlist>
<para>Printing directly to a network printer results in a printer misconfiguration
that is likely to cause a number of problems. Additionally, job options, such as copies,
the use or disuse of burst page,  and filtering, are lost.</para>
</caution>
<para>Network printers might use one or more special protocols that require a vendor-supplied
printing program. The procedures used to set up the vendor-supplied printing program
can vary. If the printer does not come with vendor– supplied support, Solaris
network printer support can be used with most devices. Use the printer vendor-supplied
software, when possible.</para>
<para>The vendor might supply an SVR4 printer interface script to replace the standard
printer interface script. If so, that SVR4 interface script calls the vendor-supplied
printing program to send the job to the printer. If not, you need to modify the standard
interface script to call the vendor-supplied printing program. To do so, edit the
per-printer copy of the standard interface script to call the vendor-supplied printing
program.</para>
<para>The following terms are used in network printer configuration:</para>
<itemizedlist>
<listitem>
<para>
<emphasis role="strong">Print server</emphasis> – The system
that spools and schedules the jobs for a printer. The print server is the system on
which the printer is configured.</para>
</listitem>
<listitem>
<para>
<emphasis role="strong">Printer-host device</emphasis> – The
software and hardware supplied by a vendor that provides network printer support for
a non-network capable printer. The combination of the printer-host device with one
or more printers attached to the device creates a <emphasis>network printer</emphasis>.</para>
</listitem>
<listitem>
<para>
<emphasis role="strong">Printer node</emphasis> – Is either
the physical printer or the printer-host device. The printer node is the physical
printer when the network support resides in the physical printer. The printer node
is the printer-host device when an external box is used to provide the network interface.
The printer node name is the system name given with the IP address. You select this
name, which has no default or vendor requirement. The printer node name, as with all
nodes, must be unique.</para>
</listitem>
<listitem>
<para>
<emphasis role="strong">Printer name</emphasis> – The name typed
on the command line when you use any of the printer commands. You select the printer
name at the time of printer configuration. Any one physical printer can have several
printer name or queue names. Each name provides access to the printer.</para>
</listitem>
<listitem>
<para>
<emphasis role="strong">Destination or network printer access name</emphasis> – The internal name of the printer node port that is used by the
printer subsystem to access the printer. The access name is the name of the printer
node, or the name of the printer node with a printer vendor port designation. Any
printer vendor port designation is explicitly defined in the printer vendor documentation.
The designation is printer specific. If the printer is a printer-host device and a
printer, the port designation is documented in the printer-host device documentation.
The following formats are used:</para>
<para>
<replaceable>printer-node-name</replaceable>
</para>
<para>
<replaceable>printer-node-ame</replaceable>:<replaceable>port-designation</replaceable>
</para>
</listitem>
<listitem>
<para>
<emphasis role="strong">Protocol</emphasis> – The over-the-wire
protocol used to communicate with the printer. The documentation from the printer
vendor  supplies the information regarding the protocol to select. The network printer
support supplies both the BSD print protocol and raw TCP. Due to implementation variations,
you might want to try both protocols.</para>
</listitem>
<listitem>
<para>
<emphasis role="strong">Timeout, or retry interval timeout</emphasis> –
Is a seed number that represents the number of seconds to wait between attempting
connections to the printer. This seed number is the smallest amount of time to wait
between attempted connections. The seed number increases when there is an increase
in failed connections. After repeated failures to connect to the printer, a message
is returned to the user requesting possible human intervention. Attempts to reconnect
continue until the connection is successful or the job is cancelled by the job owner.</para>
</listitem>
</itemizedlist>
<sect2 xml:id="printconcept-17">
<title>Printer Vendor-Supplied Software for Network Printers</title>
<para>Network printers often have software support provided by the printer vendor.
If your printer has printer vendor- supplied software, use the printer vendor software,
when possible. The software is designed to support the attributes of the printer and
can take full advantage of the printer capabilities. Read the printer vendor documentation
to install and configure the printer, so that it can operate with the LP print service.</para>
</sect2>
<sect2 xml:id="printconcept-18">
<title>Sun Support for Network Printers</title>
<para>If the network printer vendor does not provide software support, Sun supplied
software is available. The software provides generic support for network printers
and is not capable of providing full access to all possible printer attributes.</para>
<para>A general discussion of how to add a network printer is provided in <olink remap="internal" targetdoc="chapter-3.xml" targetptr="printsetup-33230">Chapter 3, Setting Up Printers (Tasks)</olink>. The following is a discussion
of printer management when you use the Sun supplied software.</para>
</sect2>
<sect2 xml:id="printconcept-19">
<title>Invoking Network Printer Support</title>
<para>The software support for network printers is called through the network interface
script <command>netstandard</command>. Configuring a network printer with this script
causes the network printer support module to be called. Here is the command to configure
the printer with the network support.</para>
<screen>lpadmin -p <replaceable>printer-name</replaceable> -m netstandard </screen>
</sect2>
<sect2 xml:id="printconcept-20">
<title>Selecting the Print Protocol</title>
<para>The print subsystem uses the BSD print protocol and raw TCP to communicate with
the printer. The printer vendor documentation provides information about which protocol
to use. In general, the TCP protocol is more generic across printers.</para>
<note>

<para>The Device URI protocol
was introduced in the Solaris Express 2/05 release. This protocol enables remote printer
access. You can specify a <replaceable>device-uri</replaceable> when adding a network
printer by using the <command>lpadmin</command> command or from within Solaris Print
Manager. For more information, see <olink remap="external" targetdoc="chapter-1.xml" targetptr="fwmxi">Device URI Protocol</olink>.</para>
</note>
<para>Type the following command to select the protocol:</para>
<screen>lpadmin -p <replaceable>printer-name</replaceable> -o protocol=bsd</screen>
<para>or</para>
<screen>lpadmin -p <replaceable>printer-name</replaceable> -o protocol=tcp</screen>
<para>If the protocol that is selected is the BSD print protocol, you can further
select the order of sending the control file to the printer. Some printers expect
the control file, then the data file. Other printers expect the reverse. For this
information, see the printer vendor documentation. The default is to send the control
file first.</para>
<para>Type the following command to select the order:</para>
<screen>lpadmin -p <replaceable>printer-name</replaceable> -o bsdctrl=first</screen>
<para>or</para>
<screen>lpadmin -p <replaceable>printer-name</replaceable> -o bsdctrl=last</screen>
</sect2>
<sect2 xml:id="printconcept-21">
<title>Selecting the Printer Node Name</title>
<para>You select the printer node name. This name must be unique, as with any node
on the network. The printer node name is associated with the IP address of the printer.</para>
</sect2>
<sect2 xml:id="printconcept-22">
<title>Selecting the Destination Name (Also Called the
Network Printer Access Name)</title>
<para>The print subsystem requires access information for the printer. The subsystem
uses  the destination name when making the network connection to the printer. You
supply this name to the print subsystem by using the <command>lpadmin</command> command.
This name then becomes part of the printer configuration database. The printer access
name is the name of the printer node, sometimes qualified by a port name. Port designation
varies across printer vendors. You can find information about port designation in
the printer documentation. Here is the format of printer access name:</para>
<para>
<replaceable>printer-node-name</replaceable>
<command>[:</command>
<replaceable>port-designation</replaceable>
<command>]</command>
</para>
<example xml:id="printconcept-23">

<title>Destination Name (or Network Printer Access Name)
With Port Designation (Number)</title>
<para>A common port designation with TCP is 9100. If the printer node name is <literal>pn1</literal>, and the printer vendor defines the port as 9100, then the printer access
name is <literal>pn1:9100</literal>. To configure a printer in this case, use the
following command:</para>
<screen>lpadmin -p <replaceable>printer_name</replaceable> -o dest=pn1:9100</screen>
</example>
<example xml:id="printconcept-24">

<title>Destination Name (or Network Printer Access Name)
With BSD Port Designation (Name)</title>
<para>When you use the BSD protocol, the port designation might not be a number, but
some name defined by the printer vendor, for example: <emphasis>xxx</emphasis>
<literal>_parallel_1</literal>. If the printer node name is <emphasis>cardboard,</emphasis> then
the printer access name is <literal>cardboard:xxx_parallel_1</literal>. To configure
a printer in this case, use the following command:</para>
<screen>lpadmin -p <replaceable>printer-name</replaceable> -o dest=cardboard:xxx_parallel_1</screen>
</example>
<example xml:id="printconcept-25">

<title>Destination Name (or Network Printer Access Name)
With No Port Designation</title>
<para>If there is no port designation, and the printer node name is <literal>newspaper</literal>, the printer access name is the printer node name: <literal>newspaper</literal>.
To configure a printer in this case, use the following command:</para>
<screen>lpadmin -p <replaceable>printer-name</replaceable> -o dest=newspaper</screen>
</example>
</sect2>
<sect2 xml:id="printconcept-26">
<title>Setting the Timeout Value</title>
<para>The timeout option allows the user to select the amount of time (in seconds)
to wait between successive attempts to connect to the printer. Some printers have
a long warm up time, and a longer timeout value is advised. The default is 10 seconds.</para>
<para>The timeout value does not impact the success or failure of the print process.
The value is a seed value that the software uses as the initial timeout count. On
repeated failures, this count is increased. A message is sent to the spooler when
repeated attempts to connect to the printer fail. This message alerts the user that
intervention might be required. The failure could be anything from the printer being
turned off to the printer being out of paper. If these messages are produced too often,
for example when the printer is warming up, increasing the timeout value will eliminate
spurious messages.</para>
<para>You ncan experiment to find the optimal timeout value. Type the following command
to set the timeout value:</para>
<screen>lpadmin -p <replaceable>printer-name</replaceable> -o timeout=<replaceable>n</replaceable>
</screen>
</sect2>
<sect2 xml:id="printconcept-27">
<title>Managing Network Printer Access</title>
<para>Each network printer should have only one server that provides access to the
printer. This restriction enables the server to manage access to the printer and keep
jobs coherent. </para>
<para>The default device for the network printer is <filename>/dev/null</filename>.
This device is sufficient when the printer has only one queue. If more queues are
required, set the device to a file. This setting enables the print system to restrict
access to the printer across queues. The following commands create a device file and
configure it as the network printer device.</para>
<screen>touch /<replaceable>path</replaceable>/<replaceable>filename</replaceable>
chmod 600 /<replaceable>path</replaceable>/<replaceable>filename</replaceable>
lpadmin -p <replaceable>printer-name</replaceable> -v /<replaceable>path</replaceable>/<replaceable>filename</replaceable>
</screen>
<para>The following is an example of how to create a device file called <literal>devtreedown</literal>.</para>
<screen># <userinput>touch /var/tmp/devtreedown</userinput>
# <userinput>chmod 600 /var/tmp/devtreedown</userinput>
# <userinput>lpadmin -p treedown -v /var/tmp/devtreedown</userinput>
</screen>
</sect2>
<sect2>
<title/>
<task id="printsetup-34420">

<title>How to Add a Network Printer by Using Printer Vendor-Supplied
Tools</title>
<indexterm xml:id="indexterm-44">
<primary>printers</primary>
<secondary>adding a network printer (how to)</secondary>
<tertiary>with Vendor Supplied Tools</tertiary>
</indexterm>
<indexterm xml:id="indexterm-45">
<primary>network printer</primary>
<secondary>adding (how to)</secondary>
<tertiary>with Vendor Supplied Tools</tertiary>
</indexterm>
<procedure>
<step xml:id="printsetup-step-22">
<para>Connect the printer to the network. Turn on the
power to the printer.</para>
<para>Consult the printer vendor's installation documentation
for information about the hardware switches and cabling requirements. Get an IP address
and select a name for the printer node. This method is equivalent to adding any node
to the network.</para>
</step>
<step xml:id="printsetup-step-23">
<para>Follow the printer vendor instructions to add
the network printer.</para>
<para>Use the printer vendor instructions to configure
the network printer. These instructions are specific to the vendor and printer.</para>
</step>
<step xml:id="printsetup-step-24">
<para>Add client access to the new printer.</para>
<para>Now that the printer has been added, create access to the printer for the print clients.
For more information, see <olink remap="internal" targetdoc="chapter-3.xml" targetptr="ermli">Setting Up a Print Client</olink>.</para>
</step>
<step xml:id="printsetup-step-35">
<para>Complete any optional tasks.</para>
<para>There
are several optional tasks that you might want to complete when setting up a network
printer. For pointers to these tasks, see <olink remap="internal" targetdoc="chapter-3.xml" targetptr="printsetup-2">Setting Up Printing (Task Map)</olink>.</para>
</step>
</procedure>
</task>
</sect2>
<sect2>
<title/>
<task id="eyasc">

<title>How to Add a New Network Printer by Using Solaris Print Manager</title>
<indexterm xml:id="indexterm-46">
<primary>Expanded Printer Support</primary>
<secondary>adding a network printer with PPD files by using Solaris Print Manager</secondary>
</indexterm>
<tasksummary>
<para>The <literal>Use PPD files</literal> default option in Solaris Print Manage
enables you to select the printer make, model, and driver when adding new printer.
For more information on this feature, see <olink remap="external" targetdoc="chapter-1.xml" targetptr="erfaz">Expanded Printer Support</olink>..</para>
<para>This procedure shows you how to add a new network printer by using Solaris Print
Manager with the Use PPD files default option selected. To add a new network printer
without PPD files, you must first deselect this option in the Print Manager drop-menu.</para>
</tasksummary>
<procedure xml:id="ertsf">
<step xml:id="ertsi">
<para>Select the system to be the print server.</para>
</step>
<step xml:id="ertsc">
<para>Connect the printer to the network. Turn on the power to the
printer.</para>
<para>Consult the printer vendor's installation documentation for information
about the hardware switches and cabling requirements. </para>
</step>
<step xml:id="ertsm">
<para>Start Solaris Print Manager on the print server.</para>
<para>For instructions, see <olink remap="internal" targetdoc="chapter-3.xml" targetptr="printsetup-6">How to Start Solaris Print Manager</olink>.</para>
</step>
<step xml:id="ertse">
<para>Select New Network Printer from the Printer menu.</para>
<para>The New Network Printer window is displayed.</para>
</step>
<step xml:id="ertsk">
<para>Fill in the required fields with the appropriate information.</para>
<para>
<indexterm xml:id="ertsh">
<primary>local or attached printer</primary>
<secondary>adding by using Solaris Print Manager (how to)</secondary>
</indexterm>
<indexterm xml:id="ertsb">
<primary>printers</primary>
<secondary>adding local or attached (how to)</secondary>
<tertiary>adding by using Solaris Print Manager</tertiary>
</indexterm>
<indexterm xml:id="ertsj">
<primary>adding</primary>
<secondary>local or attached printers (how to)</secondary>
</indexterm>If you need information to complete a field, click the Help button.</para>
<para>For example, to add a new network printer with PPD files, provide the appropriate
information in the following fields:</para>
<screen>Printer Name:
Description:
Printer Make:
Printer Model:
Printer Driver:
Fault Notification:
Destination:
Protocol:
Options:
Default Printer
Always Use Banner
User Access List:</screen>
<para>The Printer Server field is filled in by Solaris Print Manager.</para>
<para>To add a new network printer without PPD files, provide the appropriate information
in the following fields:</para>
<screen>Printer Name:
Description:
Printer Type:
File Contents:
Fault Notification:
destination:
Protocol:
Options:
Default Printer
Always Use Banner
User Access List:</screen>
<para>The Printer Server field is filled in by Solaris Print Manager.</para>
</step>
<step xml:id="ertsn">
<para>Click OK.</para>
</step>
<step xml:id="ertsd">
<para>Verify that the printer has been installed. Check for the new
printer entry in the Solaris Print Manager main window. </para>
</step>
<step xml:id="ertrz">
<para>Verify that the printer can print requests.</para>
<screen>$ <userinput>lp -d</userinput> <replaceable>printer-name</replaceable> <replaceable>filename</replaceable>
</screen>
</step>
<step xml:id="ertry">
<para>Exit Solaris Print Manager.</para>
<para>Choose Exit from the
Print Manager Menu.</para>
</step>
</procedure>
</task>
</sect2>
<sect2>
<title/>
<task id="ertsp">

<title>How to Add a New Network Printer by Using LP Print Service
Commands</title>
<indexterm xml:id="indexterm-47">
<primary>Expanded Printer Support</primary>
<secondary>adding a network printer with PPD files by using <command>lp</command> commands</secondary>
</indexterm>
<tasksummary>
<para>This procedure describes how to add a new network printer with the expanded
printer support that is available in this release. There are two examples that follow
this procedure. The first example shows you how to add a new network printer with
PPD files by using <command>lp</command> commands. The second example shows you how
to add a new network printer without PPD files by using <command>lp</command> commands. </para>
<para>The following steps are necessary to set up a network printer by using the network
printer support software. The use of this software is intended for printers that do
not come with vendor-supplied software.</para>
<para>For overview information about adding a new network printer or modifying an
existing network printer with PPD files, see <olink remap="external" targetdoc="chapter-1.xml" targetptr="printconcept-42">What's New in Printing?</olink>.</para>
<note>

<para>In this release, you can use the <command>lpadmin</command> command with the <option>
v</option> and <option>
m</option> options to specify a <replaceable>device-uri</replaceable>.
The Device URL protocol supports remote printer access. For more information and a
list of the supported <replaceable>device-uri</replaceable> destinations, see <olink remap="external" targetdoc="chapter-1.xml" targetptr="fwmxi">Device URI Protocol</olink>.</para>
</note>
</tasksummary>
<procedure xml:id="ertso">
<step>
<para>Connect the printer to the network, and turn on the power to the printer.</para>
<para>Consult the printer vendor's installation documentation for information
about the hardware switches and cabling requirements. Get an IP address and select
a name for the printer node. This step is equivalent to adding any node to the network.</para>
</step>
<step>
<para>Collect the information that is required to configure a new network printer
with PPD files.</para>
<itemizedlist>
<listitem>
<para>Printer name and port device</para>
</listitem>
<listitem>
<para>Network printer access name</para>
</listitem>
<listitem>
<para>Protocol</para>
</listitem>
<listitem>
<para>Timeout</para>
</listitem>
<listitem>
<para>File content type</para>
</listitem>
<listitem>
<para>PPD file</para>
</listitem>
</itemizedlist>
<para>To determine the PPD file, first define the printer make, model, and driver
that the printer will use.</para>
<para>For more information, see the terms that are
described in <olink remap="internal" targetdoc="chapter-3.xml" targetptr="printsetup-30">Introduction to Adding a Network Printer</olink>.</para>
</step>
<step>
<para>Define the printer name, port device, file content type, and PPD file
that the printer will use. When using PPD files, the file content type is usually
PostScript.</para>
<substeps>
<step>
<para>Specify the printer name and the port device that the printer will use.</para>
<screen># <userinput>lpadmin -p <replaceable>printer-name</replaceable> -v /dev/null</userinput>
</screen>
<para>The device to use is <filename>/dev/null</filename>.</para>
<note>

<para>Starting with the Solaris
Express 2/05 release, you can use the <option>
v</option> option to specify a <replaceable>device-uri</replaceable>. The protocol enables you to access remote printers. For
more information, see <olink remap="external" targetdoc="chapter-1.xml" targetptr="fwmxi">Device URI Protocol</olink>.</para>
</note>
</step>
<step>
<para>Specify the interface script that the printer will use.</para>
<screen># <userinput>lpadmin -p <replaceable>printer-name</replaceable> -m netstandard_foomatic</userinput> </screen>
<para>The interface script that is supplied with the support software for network
printer is <filename>/usr/lib/lp/model/netstandard_foomatic</filename>.</para>
</step>
<step>
<para>Specify the file content type and PPD file that the printer will use.</para>
<para>When using PPD files, the file content type is usually PostScript.</para>
<screen># <userinput>lpadmin -p</userinput> <replaceable>printer-name</replaceable> <userinput>-I</userinput> <replaceable>content-type</replaceable> <userinput>-n</userinput> 
/<replaceable>path</replaceable>/<replaceable>ppdfile</replaceable>
</screen>
<para>The PPD file that you specify is located in the <filename>/<replaceable>path</replaceable>
</filename> directory.</para>
<para>For more information, see <olink remap="external" targetdoc="819-2240" targetptr="lpadmin-1m">
<citerefentry>
<refentrytitle>lpadmin</refentrytitle>
<manvolnum>
1M
</manvolnum>
</citerefentry>
</olink>.</para>
</step>
<step>
<para>Specify the printer description.</para>
<screen># <userinput>lpadmin -p <replaceable>printer-name</replaceable> -D  "<replaceable>printer-description</replaceable>"</userinput>
</screen>
</step>
</substeps>
</step>
<step>
<para>Set the printer destination, protocol, and timeout values.</para>
<screen># <userinput>lpadmin -p <replaceable>printer-name</replaceable> -o dest=</userinput>
<replaceable>access-name:port</replaceable> <userinput>-o protocol=<replaceable>protocol</replaceable>
</userinput>
<userinput>-o timeout=</userinput>
<replaceable>value</replaceable>
</screen>
<variablelist>
<varlistentry>
<term>
<option>
p</option> <replaceable>printer-name</replaceable>
</term>
<listitem>
<para>Specifies the network printer name.</para>
</listitem>
</varlistentry>
<varlistentry>
<term>
<option>
o dest=</option>
<replaceable>access-name:port</replaceable>
</term>
<listitem>
<para>Sets the printer destination to the network printer access name and
a designated printer vendor port, if the port is defined in the printer vendor documentation.</para>
</listitem>
</varlistentry>
<varlistentry>
<term>
<option>
o protocol=</option>
<replaceable>protocol</replaceable>
</term>
<listitem>
<para>Sets the over-the-wire protocol used to communicate with the printer.
Both BSD and raw TCP are supported.</para>
</listitem>
</varlistentry>
<varlistentry>
<term>
<option>
o timeout=</option>
<replaceable>value</replaceable>
</term>
<listitem>
<para>Sets a retry timeout value that represents a number of seconds to
wait between attempting connections to the printer. </para>
</listitem>
</varlistentry>
</variablelist>
</step>
<step>
<para>(Optional) Add filters to the print server.</para>
<screen># <userinput>cd /etc/lp/fd</userinput>
# <userinput>for</userinput> <replaceable>filter</replaceable> <userinput>in *.fd;do</userinput>
    &gt; <replaceable>name</replaceable>
<userinput>=`basename $</userinput> <replaceable>filter</replaceable> <userinput>.fd`</userinput>
    &gt; <userinput>lpfilter -f $</userinput>
<replaceable>name</replaceable> <userinput>-F $</userinput> <replaceable>filter</replaceable>
    &gt; <userinput>done</userinput>
</screen>
</step>
<step>
<para>Enable the printer to accept print requests and to print those requests. </para>
<screen># <userinput>accept</userinput> <replaceable>printer-name</replaceable>
# <userinput>enable</userinput> <replaceable>printer-name</replaceable>
</screen>
</step>
<step>
<para>Verify that the printer is correctly configured.</para>
<screen># <userinput>lpstat -p</userinput> <replaceable>printer-name</replaceable>
</screen>
</step>
<step>
<para>Add client access to the new printer.</para>
<para>Now that the printer
has been added, create access to the printer for the clients. For more information,
see <olink remap="internal" targetdoc="chapter-3.xml" targetptr="ermli">Setting Up a Print Client</olink>. For information about
how to define a print client by using a name service, see <olink remap="external" targetdoc="chapter-1.xml" targetptr="printconcept-85">Printing Support in the Name Service Switch</olink>.</para>
</step>
<step>
<para>Complete any optional tasks.</para>
<para>There are several optional tasks
that you might want to complete when setting up a printer. For pointers to the remaining
tasks, see <olink remap="internal" targetdoc="chapter-3.xml" targetptr="printsetup-2">Setting Up Printing (Task Map)</olink>.
For information about how to define print clients by using the name service, see <olink remap="external" targetdoc="chapter-1.xml" targetptr="printconcept-85">Printing Support in the Name Service Switch</olink>.</para>
</step>
</procedure>
<example xml:id="ertsq">

<title>Adding a New Network Printer With PPD Files by Using LP Print Service Commands</title>
<para>This example shows how to add a new network printer with PPD files. The <option>
n</option> option to the <command>lpadmin</command> command enables you to add a new
print queue by specifying PPD files. The commands used in this example must be executed
on the print server. The following information is used as an example only. The information
that you provide will vary.</para>
<itemizedlist>
<listitem>
<para>Printer name : <literal>luna1</literal>
</para>
</listitem>
<listitem>
<para>Device: <literal>/dev/null</literal>
</para>
</listitem>
<listitem>
<para>Interface: <literal>/usr/lib/lp/model/netstandard_foomatic</literal>
</para>
</listitem>
<listitem>
<para>Network printer access name: <literal>nimquat:9100</literal>
</para>
</listitem>
<listitem>
<para>Protocol: <literal>tcp</literal>
</para>
</listitem>
<listitem>
<para>Timeout: 5</para>
</listitem>
<listitem>
<para>File content type: <literal>postscript</literal>
</para>
</listitem>
<listitem>
<para>PPD file: <filename>/<replaceable>path</replaceable>/<replaceable>ppdfile</replaceable>
</filename>
</para>
<screen># <userinput>lpadmin -p luna1 -v /dev/null</userinput> <lineannotation>(1)</lineannotation>
# <userinput>lpadmin -p luna1 -m netstandard_foomatic</userinput> <lineannotation>(2)</lineannotation>
# <userinput>lpadmin -p luna1 -o dest=nimquat:9100 -o protocol=tcp 
-o timeout=5</userinput> <lineannotation>(3)</lineannotation>
# <userinput>lpadmin -p luna1 -I postscript</userinput> <lineannotation>(4)</lineannotation>
# <userinput>lpadmin -p luna1 -n /<replaceable>path</replaceable>/<replaceable>ppdfile</replaceable>
</userinput> <lineannotation>(5)</lineannotation>
# <userinput>lpadmin -p luna1 -D "Second floor color printer"</userinput>
<lineannotation>(6)</lineannotation>
# <userinput>cd /etc/lp/fd</userinput>
# <userinput>for</userinput> <replaceable>filter</replaceable>
<userinput>in *.fd;do</userinput>	
	   &gt; <replaceable>name</replaceable> <userinput>=`basename $</userinput> <replaceable>filter</replaceable> <userinput>.fd`</userinput>	
	   &gt; <userinput>lpfilter -f $</userinput> <replaceable>name</replaceable> <userinput>-F $</userinput> <replaceable>filter</replaceable>	
	   &gt; <userinput>done</userinput> <lineannotation>(7)</lineannotation>
# <userinput>accept luna1</userinput>
	destination "luna1" now accepting requests
# <userinput>enable luna1</userinput> <lineannotation>(8)</lineannotation>
printer "luna1" now enabled 
# <userinput>lpstat -p luna1</userinput> <lineannotation>(9)</lineannotation>
 printer luna1 is idle. enabled since Mon Apr 19 15:31:15 2004. available.</screen>
</listitem>
</itemizedlist>
<orderedlist>
<listitem>
<para>Defines printer name and sets the device to <filename>/dev/null</filename>
</para>
</listitem>
<listitem>
<para>Defines the interface script for network printers</para>
</listitem>
<listitem>
<para>Sets the destination, protocol, and timeout</para>
</listitem>
<listitem>
<para>Specifies the file content types to which the printer can print to
directly</para>
</listitem>
<listitem>
<para>Specifies the PPD file to use</para>
</listitem>
<listitem>
<para>Adds a description for the printer</para>
</listitem>
<listitem>
<para>Adds print filters to the print server</para>
</listitem>
<listitem>
<para>Accepts print requests for the printer and enables the printer</para>
</listitem>
<listitem>
<para>Verifies that the printer is ready for printing</para>
</listitem>
</orderedlist>
</example>
<example xml:id="ertsr">

<title>Adding a New Network Printer Without PPD Files by Using LP Print Service Commands</title>
<para>This example shows how to add a new network printer without PPD files. The commands
must be executed on the print server. The following information is used as an example.
The information that you provide will vary.</para>
<itemizedlist>
<listitem>
<para>Printer name: <literal>luna1</literal>
</para>
</listitem>
<listitem>
<para>Device: <literal>/dev/null</literal>
</para>
</listitem>
<listitem>
<para>Interface: <literal>/usr/lib/lp/model/netstandard</literal>
</para>
</listitem>
<listitem>
<para>Network printer access name: <literal>nimquat:9100</literal>
</para>
</listitem>
<listitem>
<para>Protocol: <literal>tcp</literal>
</para>
</listitem>
<listitem>
<para>Timeout: 5</para>
</listitem>
<listitem>
<para>File content type: <literal>postscript</literal>
</para>
</listitem>
<listitem>
<para>Printer type: <literal>PS</literal>
</para>
</listitem>
</itemizedlist>
<screen># <userinput>lpadmin -p luna1 -v /dev/null</userinput> <lineannotation>(1)</lineannotation>
# <userinput>lpadmin -p luna1 -m netstandard</userinput> <lineannotation>(2)</lineannotation>
# <userinput>lpadmin -p luna1 -o dest=nimquat:9100 -o protocol=tcp 
-o timeout=5</userinput> <lineannotation>(3)</lineannotation>
# <userinput>lpadmin -p luna1 -I postscript -T PS</userinput> <lineannotation>(4)</lineannotation>
# <userinput>lpadmin -p luna1 -D "Room 1954 ps"</userinput> <lineannotation>(5)</lineannotation>
# <userinput>cd /etc/lp/fd</userinput>
# <userinput>for</userinput> <replaceable>filter</replaceable> <userinput>in *.fd;do</userinput>	
	   &gt; <replaceable>name</replaceable> <userinput>=`basename $</userinput> <replaceable>filter</replaceable> <userinput>.fd`</userinput>	
	   &gt; <userinput>lpfilter -f $</userinput> <replaceable>name</replaceable> <userinput>-F $</userinput> <replaceable>filter</replaceable>	
	   &gt; <userinput>done</userinput> <lineannotation>(6)</lineannotation>
# <userinput>accept luna1</userinput>
	destination "luna1" now accepting requests
# <userinput>enable luna1</userinput> <lineannotation>(7)</lineannotation>
printer "luna1" now enabled 
# <userinput>lpstat -p luna1</userinput> <lineannotation>(8)</lineannotation>
printer luna1 is idle.enabled since Feb 28 11:21 2003.available.</screen>
<orderedlist>
<listitem>
<para>Defines printer name and sets the device to <filename>/dev/null</filename>
</para>
</listitem>
<listitem>
<para>Defines the interface script for network printers</para>
</listitem>
<listitem>
<para>Sets the destination, protocol, and timeout</para>
</listitem>
<listitem>
<para>Specifies the file content type to which the printer can print directly,
and the printer type</para>
</listitem>
<listitem>
<para>Adds a description for the printer</para>
</listitem>
<listitem>
<para>Adds print filters to the print server</para>
</listitem>
<listitem>
<para>Accepts print requests for the printer and enables the printer</para>
</listitem>
<listitem>
<para>Verifies that the printer is ready for printing</para>
</listitem>
</orderedlist>
</example>
</task>
</sect2>
</sect1>
</chapter>
